require Midi;

operator entry() {
  MidiInput midi();
  report(midi.getPortCount());
  report(midi.isPortOpen());
  midi.openPort();
  report(midi.isPortOpen());
  midi.ignoreTypes(false, false, false);
  Byte message[];
  midi.getMessage(message);
  report(message);
  report('completed!'); 
}